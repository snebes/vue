{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
        <button type="submit">Submit</button>

        {{ form_row(form.color) }}

    {{ form_end(form) }}
{% endblock %}

{% block dumper %}
    {{ dump(form) }}
    {{ dump(data) }}
{% endblock %}
